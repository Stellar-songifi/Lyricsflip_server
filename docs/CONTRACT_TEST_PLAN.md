# Escrow Contract Test Plan

Coverage targets:

- Stake creation and cancellation.
- Winner payout with optional fee.
- Trustline failure paths.
- Config reads and admin-only config updates.

Add each regression case beside `contracts/lyricsflip-escrow/src/test.rs`.
