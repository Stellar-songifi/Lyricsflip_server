[package]
name = "lyricsflip-escrow"
version = "0.1.0"
edition = "2021"
publish = false
description = "Escrow contract for LyricsFlip wagered matches on Stellar"

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk = "22"

[dev-dependencies]
soroban-sdk = { version = "22", features = ["testutils"] }
